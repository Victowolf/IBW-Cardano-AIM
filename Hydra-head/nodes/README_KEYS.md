Place your hydra signing/verif keys in each nodes/*/keys/ directory.
Example files expected per node:
- keys/hr.sk   (hydra signing key)
- keys/hr.vk   (hydra verification key)
You can generate keys from hydra-tools or follow the hydra repo docs.
