#!/usr/bin/env bash
set -euo pipefail
echo "Starting hydra-node (placeholder) for $NODE_ID on port $PORT"
# In a real image you would run: hydra-node --config /app/nodes/$NODE_ID/config.yaml --node-socket $CARDANO_SOCKET
sleep infinity
